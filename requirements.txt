# 공시 Agent — 실행에 필요한 꾸러미
#
# 판은 개발에 쓴 것을 하한으로 적었다. 상한을 안 막은 이유는 대회 기간이
# 짧아 큰 판 변경이 일어날 여지가 적기 때문이다.
#
# 열쇠는 여기 없다. `env.example` 을 보고 `.env` 를 만들어야 한다.

# 평가용 API 서버
fastapi>=0.141
uvicorn>=0.47

# 검색
numpy>=2.3          # 벡터 내적. 임베딩 유사도 계산
rank_bm25>=0.2.2    # 낱말 검색
kiwipiepy>=0.23     # 한국어 형태소 분석. 질의와 본문을 같은 기준으로 자른다

# 외부 호출
requests>=2.32      # 표준 라이브러리로도 되지만 일부 도구에서 쓴다

# 원문 처리 (DB 를 새로 만들 때만 필요하다)
lxml>=5.3           # 공시 XML 파싱
pandas>=2.3         # 표 정리
openpyxl>=3.1       # 절 스키마·매칭표 엑셀 읽기
pypdf>=6.11         # 대체 수집분 PDF 본문
PyMuPDF>=1.27       # PDF 책갈피로 절 자르기. import 이름은 fitz 다
