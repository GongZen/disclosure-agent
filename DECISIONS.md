# 결정 로그 (추가만 한다. 수정·삭제 금지. 번복도 새 항목으로)

### 2026-08-12 CLAUDE.md를 전역·프로젝트 2파일로 분리
결정: 범용 행동 규칙은 `~/.claude/CLAUDE.md`(전역), 프로젝트 전용 사항은 `AI_Festival/CLAUDE.md`에 둔다.
이유: 매 프로젝트마다 CLAUDE.md를 새로 작성하는 번거로움을 없애기 위함. 전역 파일은 모든 프로젝트에 자동 적용되므로 복사도 불필요하다.
기각한 대안: 한 파일의 상단/하단으로 나누고 상단만 복사해 재사용하는 방식.

### 2026-08-12 승인 게이트를 3단계로 구성
결정: 행동을 자유(읽기·탐색) / 자유롭게 하되 보고(되돌릴 수 있는 로컬 변경) / 사전 승인 필수(프로젝트 파일 변경, 삭제·덮어쓰기, 외부 전송, 비용 발생)로 나눈다. 판단 기준은 가역성·부수효과 범위·비용.
이유: 2단계(승인 필요/불필요)로만 나누면 라이브러리 설치 같은 되돌릴 수 있는 작업까지 승인 대상이 되어 작업 속도가 떨어진다.
기각한 대안: 승인 필요/불필요 2단계 구분.

### 2026-08-12 승인 단위를 계획서 1건으로 하고 파일 목록을 권한 범위로 삼음
결정: 3단계 행동은 계획서를 먼저 제출한다. 계획서에 명시한 파일 목록이 곧 승인 범위이며, 목록 안은 재승인 없이 진행하고 목록 밖으로 나가야 하면 중단하고 재승인을 받는다. 계획서에 코드는 넣지 않는다.
이유: 파일 하나마다 승인을 받으면 게이트가 작업을 막는 장애물이 되고, 한마디 승인으로 무제한 작업하면 게이트가 무의미해진다.
기각한 대안: 파일 단위 승인, 작업 전체 일괄 승인.

### 2026-08-12 답변 형식을 사실·판단축·비교 3단으로 고정하고 결론은 요청 시에만 제시
결정: ①사실 ②판단 축 ③비교 분석까지만 제시하고 멈춘다. 결론·추천은 "결론만 알려줘" 등의 요청이 있을 때만 낸다. ③에서 우열을 판정하지 않고, 목록이 전부인 척하지 않으며, 비교를 대칭으로 쓴다. 단 규칙 위반이나 기술적 불가능은 의견이 아니라 사실이므로 ①에서 명시한다.
이유: 선택지를 먼저 제시받으면 그 선택지에 앵커링되어 다른 가능성을 탐색하기 어려워진다. 동시에 선택지 간 비교 분석 자체는 판단에 필요하다.
기각한 대안: 추천안 1개를 매번 함께 제시하는 방식.

### 2026-08-12 설명 방식 — 평소에는 기술 용어를 그대로 쓰고 2단 설명은 요청 시에만
결정: 평소 답변은 기술 용어를 그대로 사용하되 표현만 쉽고 간결하게 한다. 쉬운 설명 → 정석 설명의 2단 구조는 요청받을 때만 수행하며 순서를 강제한다. 비유는 허용하되 들어맞지 않는 지점을 반드시 밝힌다.
이유: 쉽게 설명하라는 것이 정식 용어까지 풀어쓰라는 뜻은 아니다. 용어를 알아야 문서 검색과 팀 논의가 가능하다.
기각한 대안: 항상 2단 설명, 쉬운 말만 사용, 용어에 괄호 풀이 병기.

### 2026-08-12 비판적 검토를 기본 태도로 하고 입장 변경 문턱을 설정
결정: 무조건적 찬성을 금지하고 사용자 의견의 허점을 먼저 검토한다. 단 억지 반론은 만들지 않는다. 새로운 사실이 제시되거나 논리의 결함이 지적될 때만 입장을 바꾸고, 반박이 정당하지 않다고 판단하면 끝까지 의견을 고수한다.
이유: 사용자가 반박하면 근거 없이도 입장을 바꾸는 경향이 관찰되었다. 반대로 무조건 적대적으로 대응하면 타당한 의견에도 억지 반론이 붙어 진짜 문제 제기가 묻힌다.
기각한 대안: 항상 반론 제시, 문턱 없는 유연한 입장 변경.

### 2026-08-12 DECISIONS.md 자동 기록을 상시 승인, 파일 인용 전 재읽기를 상시 규칙화
결정: `/decision` 호출 외에도 되돌리기 어려운 결정이 확정되면 자동으로 기록한다. 이 쓰기는 승인 게이트의 상시 예외다. 또한 특정 파일을 근거로 답변할 때는 인용 전에 그 파일을 다시 읽어 최신 버전을 확인한다. 프로젝트 전체 확인은 요청 시에만.
이유: 수동 기록만으로는 호출을 잊는 순간 결정이 유실되고 유실 사실조차 알 수 없다. 파일 하나 재읽기는 비용이 거의 없는 반면, 낡은 기억으로 답변하는 손실은 크다.
기각한 대안: 전면 수동 기록, 매 턴 프로젝트 전체 파일 스캔.

### 2026-08-12 프로젝트 CLAUDE.md는 라우팅 문서로 두되 금지 조항 4개만 본문에 직접 명시
결정: 프로젝트 CLAUDE.md에는 어떤 사항에 어떤 문서를 참고할지 라우팅만 담는다. 단 대회 규칙상 절대 위반 불가인 4개 항목(HyperCLOVA X 외 LLM 금지, 제공 코퍼스 외 데이터 금지, 외부 공시 API 실시간 호출 금지, 근거 없는 미래 예측·투자 의견 금지)은 본문에 직접 명시한다. 상세 내용은 `docs/BRIEF.md`로 분리한다.
이유: 라우팅은 "찾아보면 되는 정보"에 적합하나, "몰라서 어기면 실격되는 제약"에는 부적합하다. 위반 여부를 의심조차 하지 않으면 문서를 보러 가지 않는다.
기각한 대안: 모든 내용을 라우팅으로 처리, 모든 내용을 CLAUDE.md 본문에 포함.

### 2026-08-13 재무 수치 추출의 1차 경로를 XBRL 속성(ACONTEXT·ADECIMAL)으로 한다
결정: 사실 테이블의 기간·회계연도·연결/별도·단위 필드를 표 헤더가 아니라 TE 태그의 `ACONTEXT`(CFY/PFY/BPFY + d/e + ConsolidatedMember/SeparateMember)와 `ADECIMAL`(-6/-3/0)에서 파생시킨다. 표 파싱은 이 속성이 없는 문서에만 적용하는 2차 경로로 둔다.
이유: 표 파싱의 3대 실패 지점인 계정명 표기 차이(6종 이상, 17% 미매칭), 단위 혼재(백만원 47.3%·천원 32.8%·원 8.8%), 한 표 3개년 열 매핑이 이 두 속성으로 전부 결정론적으로 해소된다. 사업보고서 30건 실측에서 ifrs 태그의 74.6%가 ACONTEXT를 보유했고, ADECIMAL 분포(-6이 41,210건)가 표 헤더에서 별도로 센 단위 분포와 일치해 교차검증됐다.
기각한 대안: 전 문서를 표 파싱으로 일괄 처리, 원본 XML에 태그를 삽입해 저장(근거 제시 시 원문이 아니게 되고 5.5GB 재작성 비용 발생).

### 2026-08-13 제공 원문은 읽기 전용으로 두고 파생 데이터만 새로 만든다
결정: `assets/공시/공시/corpus/` 아래의 원문 XML·HTML, `manifest.jsonl`, `universe.csv`는 어떤 경우에도 수정·삭제하지 않는다. 사실 테이블·벡터 인덱스·파싱 결과 등 우리가 만드는 모든 산출물은 `data/` 아래에만 생성한다. 원문 접근은 `src/corpus.py`의 읽기 함수를 통해서만 하고 쓰기 경로를 두지 않는다.
이유: 모든 답변에 근거 공시를 표시해야 하므로 원문이 변형되면 그것은 더 이상 근거가 아니다. 파싱 규칙이 틀렸을 때 다시 뽑을 기준도 원문뿐이며, 추출값 검증도 원문 대조로만 가능하다.
기각한 대안: 원본 XML에 XBRL 태그를 삽입해 저장(근거 제시 시 원문이 아니게 되고 5.5GB 재작성 비용 발생).

### 2026-08-13 평가용 API 서버 OS를 Windows Server 2022로 유지한다
결정: NCP에 생성한 서버(cpai, s2-g3a 2vCPU/8GB, 스토리지 30GB, win-2022-en-base)를 Linux로 교체하지 않고 그대로 사용한다. 공인 IP 할당과 ACG 설정(Inbound 80·443·3389·22, Outbound 전체)은 완료됐다.
이유: 팀 두 명 모두 Linux 사용 경험이 전무하다. 마감까지 24일뿐이어서 데이터 구조화와 병행해 Linux를 익힐 여유가 없다고 판단했다. Linux 전환의 이점(OS 라이선스 비용 없음, Docker 네이티브, systemd 자동 재시작, 메모리 여유)보다 학습 곡선 리스크를 크게 봤다.
기각한 대안: Ubuntu 22.04 LTS로 재생성(전환 비용이 현시점 30분~1시간으로 가장 낮았으나 학습 부담을 이유로 기각).

### 2026-08-13 Windows 유지에 따라 서비스 자동 재시작 검증을 선행 과제로 둔다
결정: 09.07~09.20 서버 상시 가동 요구에 대응하기 위해, API 서비스의 부팅 시 자동 시작과 비정상 종료 시 자동 재시작 설정을 파이프라인 관통 직후 검증한다. 서버 재부팅 테스트로 실제 복구를 확인하기 전까지 다음 단계로 넘어가지 않는다.
이유: 이 요구는 부분 실패가 없다. 20일 중 하루라도 응답이 없으면 그 시간대 평가 요청이 전부 실패한다. Windows는 systemd 같은 표준 수단이 없어 별도 도구(NSSM 등)나 작업 스케줄러를 써야 하고 참고 사례가 적어, 마감 직전에 발견하면 대응 시간이 없다.
기각한 대안: 개발 완료 후 배포 단계에서 처리(발견 시점이 늦어 복구 시간이 없음).

### 2026-08-13 동일 XBRL 태그의 중복 등장은 ACONTEXT 축과 표 제목으로 구분한다
결정: 같은 요소명(예: `ifrs-full_Revenue`)이 재무제표 본문과 주석에 반복 등장하므로, ACONTEXT의 축 부분이 연결/별도 2종뿐인 것만 재무제표 본문 값으로 채택하고 부문축·지역축이 추가된 것은 분해 값으로 분리한다. 축이 2종이면서 본문이 아닌 경우가 남으므로 표 제목(TITLE)을 보조 조건으로 함께 건다.
이유: 사업보고서 20건 실측에서 `ifrs-full_Revenue`가 손익계산서보다 부문정보·수익 주석 표에 더 많이 붙었다(영업부문 82회 대 연결 손익계산서 36회). 축 종류가 손익계산서 안은 2종, 그 외 표는 146종으로 갈려 축만으로 대부분이 걸러지나, 그 외 표에서도 축이 2종인 사례가 56건 있어 축 단독으로는 불완전하다.
기각한 대안: 태그명만으로 값을 채택(부문별·지역별 분해 값이 전체 매출로 섞여 들어감), 표 제목만으로 판정(제목 표기가 기업마다 달라 누락 발생).

### 2026-08-14 확인 불가를 네 갈래로 나누고 각각 다른 문구로 답한다
결정: 답하지 못하는 경우를 `out_of_scope`(범위 밖) · `no_document`(문서 없음) · `not_disclosed`(미기재) · `extract_failed`(추출 실패) 넷으로 구분한다. 넷째 문구에는 "Agent가 확인하지 못했습니다"로 주체를 명시해 우리 한계임을 드러낸다. 답변은 [범위][답변][근거] 제목형 3블록으로 쓰고 한계를 앞에 밝힌다. 한계가 없으면 범위 블록을 생략한다.
이유: 문서 존재 여부 자체가 정보를 담는다. 현대자동차 거래소공시 0건은 "공시 의무가 생길 규모의 계약이 없었다"는 뜻이고 그 자체가 답인 반면, 문서는 있는데 항목이 없는 것은 다른 정보다. 그리고 우리가 못 읽은 것을 "기재되지 않았다"로 답하면 지표 7의 한계 고지가 아니라 지표 4의 거짓 진술이 된다. 한계를 뒤에 붙이면 읽는 사람이 놓치고 답변 범위를 모른 채 읽게 된다.
기각한 대안: 세 갈래(문서 없음과 미기재를 합침), 한계를 답변 뒤에 배치, 문장형 서술.

### 2026-08-14 미기재와 추출 실패는 3층 확인으로 가른다
결정: 값 추출에 실패하면 XBRL 태그 → 섹션 존재 → 항목 키워드 순으로 확인해 `not_disclosed`와 `extract_failed`를 판정한다. 1층과 2층에서 확정되면 3층을 건너뛴다.
이유: 파서 입장에서 두 경우가 똑같이 "못 찾음"으로 보이므로 1차 실패만으로는 구분이 불가능하다. XBRL 태그는 있는 문서에서 해당 태그가 없으면 회사가 보고하지 않은 것이라 확정적이고, 섹션 부재도 확정적이다(금융지주사에는 "원재료 및 생산설비" 섹션이 아예 없다). 싸고 확정적인 순서로 배치해 가장 비싼 전문 검색이 가장 적게 돌게 했다.
기각한 대안: 구분 포기하고 하나로 통합, XBRL → 키워드 → 섹션 순서(섹션이 키워드보다 싸고 부재 시 확정적이므로 앞으로 당김).

### 2026-08-15 정정 연결에서 후보를 거를 때 계약 식별 항목만 쓴다
결정: 같은 날 여러 계약이 공시되면 유형 필터만으로 후보가 좁혀지지 않으므로 계약 식별 항목으로 거른다. 식별 항목은 계약 서식이 체결계약명·수주일자, 신규시설투자 서식이 투자목적·이사회결의일이다. 지목일 후보를 좁힐 때는 둘 다 어긋나야 빼고, 지목일을 버리고 코퍼스 전체에서 내용으로 찾을 때는 하나 이상이 실제로 일치해야 채택한다.
이유: 계약금액과 매출액대비는 정정으로 자주 바뀌고 서로 연동돼 있어 하나의 정정을 둘로 세게 만든다. 이 둘을 대조에 넣었더니 멀쩡한 연결 23건이 다른 계약으로 판정됐다. 반대로 식별 항목을 "어긋나지 않으면 통과"로만 쓰면 근거 없이 이어진다. 현대건설 파나마 메트로 정정본이 샤힌 프로젝트 원본에 붙은 것은 원본에 수주일자가 없어 어긋난 항목이 계약명 하나뿐이었기 때문이다.
기각한 대안: 모든 필드를 대조(연결 22건 손실), 어긋난 항목 수만 세기(연동 필드를 중복 계산), 계약명 단독 사용(정정 시 21.7%가 바뀌고 유일하지도 않다).

### 2026-08-15 계약명의 표기 차이는 같은 계약으로 본다
결정: 띄어쓰기·괄호 앞 공백·"사업" 같은 접미어 유무만 다른 계약명은 동일 계약으로 취급한다. W4에서 `event_contract`를 만들 때 계약명을 그대로 키로 쓰지 않고 정규화한 값을 쓴다. 정규화 규칙은 D6 주요 계약의 기준을 정할 때 함께 확정한다.
이유: 전수 확인 결과 계약명만 다른 연결이 실제로 같은 계약이었다. `차륜형장갑차 4차양산`과 `차륜형장갑차 4차 양산사업`, `… 패키지 EPC 사업 (Tobelo)`와 `… 패키지 EPC 사업(Tobelo)`가 그 예다. 표기를 그대로 키로 쓰면 한 계약이 둘로 쪼개져 이력 조회가 끊긴다.
기각한 대안: 계약명 완전 일치를 요구(같은 계약이 분리됨), 유사도 임계값으로 자동 판정(임계값을 정할 근거가 없고 오병합 위험).

### 2026-08-15 원문의 값 오류는 데이터 계층에서 판정하고 LLM에 넘기지 않는다
결정: 공시 원문의 기재 오류(자기 접수일을 원본 제출일로 적은 정정본, 계약 종료일보다 뒤인 수주일자, `2043-05-31` 같은 미래 날짜)는 적재 시점에 판정해 `doc_relation`에 결과만 남긴다. HyperCLOVA X는 어느 문서가 어느 문서를 고쳤는지, 미연결 사유가 무엇인지를 다시 판단하지 않는다.
이유: 모델은 요청마다 조각만 받으므로 판단 근거가 원리적으로 부족하다. 두산퓨얼셀 수주일자가 오타임을 알려면 같은 문서의 계약 종료일과 정정본의 정정사항 표를 함께 봐야 하는데, 검색된 조각에 그 둘이 함께 들어간다는 보장이 없다. 그리고 매 요청마다 판단하면 같은 질의에 다른 답이 나와 재현성이 없다.
범위: 이 결정은 문서 간 관계에만 적용된다. 값 자체의 판본 선택은 W4·W5에서 별도로 정한다. `doc_relation`에는 계약금액도 수주일자도 들어 있지 않다.

### 2026-08-15 D1 자금조달의 범위를 확정하고 자기주식처분은 목적이 자금 확보인 2건만 포함한다
결정: 유상증자·CB·EB에 더해 상각형조건부자본증권 71건과 자본으로인정되는채무증권 6건을 자금조달에 포함한다. 자기주식처분은 유형 단위로 제외하되 처분목적이 자금 확보인 2건(엘에스일렉트릭 투자재원 확보, 한미반도체 신규장비 개발자금 확보)만 건 단위로 포함한다. 무상증자·감자는 제외한다. 상각형조건부자본증권을 자본·부채·메자닌 중 어디로 분류할지는 보류하고, 상위 카테고리를 요구하는 질의가 관측되면 재개한다.
이유: 상각형조건부자본증권은 서식에 `3. 자금조달의 목적` 항목이 있어 서식 스스로 자금조달이라고 말한다. 자기주식처분 157건을 전수로 열어 확인한 결과 실제 사건은 144건이고 그중 현금이 회사로 들어온 것은 5건뿐이며, 나머지 3건은 단주 처리와 주식매수청구권 취득분의 법정 기한 내 처분으로 금액이 최대 3.7억이다. 돈이 들어왔다는 것과 돈을 구하려 했다는 것은 다르므로 목적까지 자금조달인 2건만 남겼다. 분류를 보류한 것은 상위 카테고리가 답변에 등장하는 질의가 참고 질의 6개에 없고 서식에 자본·부채 표시가 없어 코퍼스 안에 근거가 없기 때문이다.
기각한 대안: 예시 4종만 포함(상각형조건부자본증권 71건을 놓침), 현금 유입 5건 전부 포함(법정 의무 이행을 자금조달로 셈), 자기주식처분 157건 전부 포함(통상 의미와 멀어짐), 현금흐름표 재무활동 계상 여부를 판정 기준으로 사용(연간 총액이라 개별 처분 건과 대응되지 않음).

### 2026-08-15 판정 결과가 아니라 판정 재료를 적재한다
결정: `event_funding`에 자기주식처분의 처분목적 원문과 처분방법 네 갈래 주식수를 그대로 넣는다. "이것은 자금조달이다"라는 라벨을 적재 시점에 굳히지 않고 조회 시점 규칙으로 둔다.
이유: D1의 범위가 바뀌면 라벨은 전부 다시 만들어야 하지만 원문은 그대로 쓸 수 있다. `GLOSSARY.md`가 "정의를 바꾸면 구현물만 다시 만들면 된다"고 적어둔 원칙이 성립하려면 적재물이 라벨이 아니라 원문이어야 한다. 그리고 자기주식처분처럼 같은 유형 안에서 답이 갈리는 경우 유형 이름표 28행으로는 표현되지 않는다.
기각한 대안: `document.category`에 자금조달 라벨을 박아 적재(범위 변경 시 재적재).

### 2026-08-15 D6 주요 계약에 금액 기준선을 두지 않고 공시 구분을 컬럼으로 남긴다
결정: 거래소 단일판매·공급계약 공시를 금액이나 매출액 대비 비율로 거르지 않는다. 공시된 것 전부가 대상이다. 대신 `event_contract`에 의무공시와 자율공시를 구분하는 컬럼을 둔다.
이유: 우리가 선을 그으면 공시에 있는 기준이 아니라 우리가 만든 기준으로 답을 버리게 되고, 그것은 지표 7의 한계 고지가 아니라 임의 누락이다. 당초 근거였던 "공시 의무는 일정 규모를 넘을 때 생기므로 공시된 것은 이미 규모 요건을 통과했다"는 1,169건 전수 확인 결과 의무공시에만 성립했다. 자율공시가 섞여 있고 규모 요건과 무관하게 제출된다. HD현대일렉트릭 20240830 건은 매출액 대비 2.45%로 대규모법인 의무 기준 2.5%에 미달하는데도 공시됐다. 그래서 근거를 규모 요건에서 "공시된 거래는 전부 답변에 쓸 수 있어야 한다"로 바꾸고, 대신 구분 정보를 조회 가능한 형태로 남긴다. 지금은 이 정보가 문서 제목 문자열에만 있다.
기각한 대안: 매출액 대비 n% 이상만 포함(기준선의 근거가 없고 답을 임의로 버림), 자율공시 제외(공시된 거래를 답변에서 빼게 됨), 공시 구분을 저장하지 않음(답변에서 규모 요건 미달임을 밝힐 수 없음).

### 2026-08-15 계약명 정규화는 공백 제거와 "사업" 접미 제거까지만 한다
결정: 계약명을 키로 쓸 때 유니코드 정규화·앞뒤 공백 제거·모든 공백 제거·끝의 "사업" 한 번 제거까지만 적용한다. 괄호 안 내용과 구분기호는 그대로 둔다.
이유: 1,169건 전수에서 단계별 병합 결과를 실측했다. 공백 제거는 5개 그룹을 묶었고 전부 같은 계약이었다. "사업" 제거는 1개 그룹을 묶었고 정당했다(현대로템 차륜형장갑차 4차양산 / 4차 양산사업). 반면 괄호 제거는 10개 그룹을 묶었는데 대부분 다른 계약이었다. 한전기술의 (Tobelo)와 (Sumbawa)는 다른 지역, 현대건설의 (PKG CP S-04·05·06)은 다른 공구, 현대로템의 (Lot2:전기기관차)와 (Lot3:전동차)는 다른 품목이다. 괄호 안이 계약을 구분하는 정보다. 구분기호 통일과 공사·계약·건·외 접미 제거는 병합 건수가 0이라 위험만 있고 이득이 없다.
기각한 대안: 괄호 안 제거(다른 계약 10그룹이 뭉침), 접미어를 넓게 제거(효과 0에 오병합 위험만 추가), 유사도 임계값 자동 판정(임계값 근거가 없고 오병합 위험).

### 2026-08-15 사실 계층 적재를 여섯 걸음으로 나누고 걸음마다 통과 조건을 숫자로 정한다
결정: W4 이후 모든 적재 작업을 서식 판별 → 항목 사전 → 값 추출 → 검산 → 적재 → 검증 여섯 걸음으로 나눈다. 값을 뽑는 일과 DB에 넣는 일을 붙이지 않고, 각 걸음의 통과 조건을 숫자로 미리 정해 "괜찮아 보인다"로 넘어가지 않는다. 검증은 원문 품질이 아니라 우리 파서를 검사하는 것으로 정의하고, 원문 자체의 기재 오류는 실패에서 빼되 목록으로 출력한다. 방식은 `docs/feedback/README.md`에 적었다.
이유: W3에서 적재를 열두 번 다시 했는데, 값 추출과 적재가 붙어 있어 틀린 값이 들어간 뒤에야 틀렸음을 알았기 때문이다. W4에서 걸음을 나눠 보니 각 걸음에서만 잡히는 오류가 실제로 있었다. 1번에서 항목명 앞 하이픈 누락으로 1,083건이 미분류로 드러났는데, 곧바로 값을 뽑았다면 빈 행 1,022개가 적재된 뒤에 알았을 것이다. 2번에서는 전건에 있어야 할 항목이 71%로 나와 정규식 겹침 문제가 드러났다. 검증의 성격을 바꾼 것은, 회사가 안 쓴 칸을 실패로 세면 매번 실패가 떠서 검증 결과를 보지 않게 되기 때문이다.
기각한 대안: 추출과 적재를 한 스크립트에서 처리(W3 방식. 재적재 반복), 검증에서 빈 값을 일괄 실패 처리(원문 품질과 파서 성능이 섞임), 원문 오류를 알려진 목록으로 하드코딩해 무시(새 오류가 생겨도 안 보임).

### 2026-08-15 원문의 저장 구조를 먼저 확인하고 그 구조에 맞는 스키마를 정한다
결정: 적재 작업의 0번 걸음으로 원문 구조 확인을 둔다. 저장 형식·파싱 가능 여부·표와 절의 구조·태그가 담은 정보를 먼저 보고, 그 구조를 살리는 추출 방식과 스키마를 정한 뒤에 작업을 시작한다. 앞 작업에서 정한 스키마를 다른 문서 종류에 그대로 적용하지 않는다. 구조를 손상 없이 담는 것이 우선이고 조회 편의는 그다음이다.
이유: W3과 W4 계약에서 원문을 평문으로 만들어 "항목명 다음 줄이 값" 규칙으로 읽었는데, 확인해 보니 주요사항보고서·거래소공시·지분공시 셋 다 표 구조를 갖고 있었다. 평문화하면 그 구조가 사라져서 본문 문장 조각이 항목으로 잡히고, 세로 병합된 칸의 항목명이 없어지고, 한 행에 값이 둘 이상일 때 하나를 잃는다. 구조를 그대로 읽으면 첫 칸이 항목이고 뒤가 값이라는 것이 문서 형식으로 보장되므로 우리가 규칙을 만들어 추측할 필요가 없다. `PLAN.md`에 "거래소공시는 확장자만 .xml이고 실제로는 HTML"이라고 적어두고 구조를 못 쓴다고 짐작했는데, HTML에도 table·tr·td가 있어 400건 전부 파싱됐다. 그리고 같은 W4 안에서도 계약(유형 3개·서식 5개·컬럼 27개)과 주요사항(유형 28개·고유 항목 678개·한 문서에 같은 항목 최대 39회 반복)은 구조가 전혀 달라, 앞의 스키마를 그대로 가져갔으면 대부분을 버렸을 것이다.
기각한 대안: 평문 파싱을 계속 사용(구조 정보 손실), 한 스키마를 모든 문서 종류에 적용(문서마다 구조가 달라 손실 발생), 구조 확인을 파서 작성과 동시에 진행(구조를 모른 채 스키마를 정하게 됨).

### 2026-08-17 D4 연결을 재무 수치의 기본 기준으로 한다
결정: 질의에 명시가 없으면 연결 기준으로 답하고 답변에 "연결 기준"이라고 밝힌다. 별도를 물으면 별도로 답한다. 둘을 함께 제시하지 않는다.
이유: 참고 질의가 "2025년 연결기준 매출액"처럼 연결을 지목했고, 기업 실적을 말할 때 통상 연결을 본다. 자회사를 포함한 그룹 전체의 살림이기 때문이다. 둘 다 제시하면 묻지 않은 정보가 붙어 읽는 사람이 어느 쪽이 답인지 판단해야 한다.
기각한 대안: 둘 다 제시(답변이 길어지고 질문에 없던 정보가 붙음), 별도를 기본으로(통상 기준과 어긋남).

### 2026-08-17 매출액과 영업수익을 같은 항목의 다른 표기로 보지 않는다
결정: D3 계정 체계 통합의 질문을 "같은 항목의 여섯 가지 표기를 어떻게 묶을 것인가"에서 "업종에 따라 다른 이 계정들을 하나로 묶어도 되는가, 묶는다면 어떤 근거로 묶고 답변에서 무엇이라고 부를 것인가"로 바꾼다. 묶을 근거의 후보는 XBRL 태그이며, 서로 다른 계정명이 같은 태그를 달고 있는지 W5 2번 걸음에서 실측한 뒤 정한다.
이유: 사용자 지적. 매출액과 영업수익은 정의상 다른 계정이다. 재화 판매 수익과 서비스·금융업 영업 수익을 회계 기준이 업종에 따라 다르게 표시하도록 정한 것이지 표기 취향의 문제가 아니다. 이것을 표기 변형으로 취급하면 "삼성전자와 HMM의 매출을 비교해달라"는 질의에 근거 없이 두 값을 나란히 놓게 된다.
기각한 대안: 여섯 표기를 사전으로 통합(정의가 다른 계정을 같은 것으로 취급하게 됨).

### 2026-08-17 걸음을 일곱으로 늘리고 탐색 국면과 구현 국면을 분리한다
결정: 2026-08-15 항목의 여섯 걸음에 0번(원문 구조 확인)을 추가해 일곱으로 한다. 그리고 탐색과 구현을 국면으로 나눈다. 탐색 국면의 코드는 버릴 셈으로 짜고 결과물은 "무엇이 몇 가지이고 어떤 분포인가"라는 사실이다. 구현 국면은 탐색이 끝난 뒤 시작하며 의사결정과 돌발 대응이 없어야 한다. 구현 중에 예상 못 한 구조를 발견하면 그 자리에서 코드를 고치지 않고, 멈춰서 탐색 국면으로 되돌아가 그 유형의 분포를 전수로 센 뒤 규칙을 다시 정한다. (2026-08-15 "사실 계층 적재를 여섯 걸음으로 나누고" 항목을 보완)
이유: W5에서 `fsdoc.py`를 다섯 번 고쳤는데 전부 같은 성격이었다. 원문 한 건을 보고 구조를 단정한 뒤 전수로 확인하지 않았고, 실패하면 그 사례만 막는 수정을 했다. 제목 표가 칸 1개인 형태만 있는 줄 알았다가 칸 4개 형태를 놓쳤고, 단위를 표 앞 3행에서만 찾다가 넷째 행에 있는 것을 놓쳤고, `_first_row`가 첫 행만 읽어 또 놓쳤다. 고치면 그 사례만 막히고 같은 유형이 또 나오지만, 되돌아가 분포를 세면 유형 전체가 정리된다. 전수 집계 비용도 문제가 아니다. 정규식 집계가 1,054건에 37~54초, 파싱까지 해도 3~7분이다.
기각한 대안: 탐색에서 전부 알아내 구현에서 돌발을 0으로 만든다(문서를 만든 쪽이 우리 예상과 무관하게 만들므로 도달 불가), 지금처럼 구현 중 즉시 수정(같은 유형이 반복 출현).

### 2026-08-17 통과 조건을 구조 조건·정지 조건·품질 하한선 셋으로 나눈다
결정: 2026-08-15 항목의 "각 걸음의 통과 조건을 숫자로 미리 정한다"를 셋으로 나눈다. 구조 조건은 데이터가 어떻게 생겼든 반드시 성립해야 하는 것으로 미리 정하고 어기면 멈춘다(중복 0, 참조 무결성 0, 파싱 실패 0, 문서당 한 행, 불일치 건 전건 원인 규명). 커버리지는 숫자로 미리 정하지 않고 정지 조건으로 대신한다(원인이 원문 특성이면 멈추고 not_disclosed로 표시, 우리 파서 문제면 고침, 고치는 비용이 크면 판단을 사용자에게 올림). 품질 하한선은 유지한다(자기 검산 일치율, 회계 항등식, 골든 데이터셋 대조). (2026-08-15 항목을 보완)
이유: 미리 정할 수 있는 것과 데이터를 봐야 아는 것이 섞여 있었다. 커버리지를 "95% 이상"처럼 미리 박으면 실제 분포가 다를 때 그 숫자가 방향을 왜곡한다. W5 1번 걸음에서 재무제표를 못 찾은 문서가 155건 나왔을 때, 목표 수치가 있었다면 그것을 채우려 무리한 규칙을 만들었을 것이다. 실제로는 원인이 목차 구조 변화였고 규칙을 바꾸니 3건으로 줄었다. 반면 품질 하한선은 성격이 다르다. "얼마나 많이 뽑았나"가 아니라 "뽑은 값이 맞나"이므로 목표가 아니라 경보선이고, 낮으면 파서가 틀린 것이다.
기각한 대안: 모든 조건을 숫자로 미리 정함(커버리지가 왜곡됨), 모든 조건을 데이터를 보고 정함(구조 조건마저 흔들려 "이 정도면 됐다"로 넘어가게 됨).

### 2026-08-17 골든 데이터셋을 만들어 파서 수정의 기준으로 삼는다
결정: 사람이 원문을 직접 읽어 값을 적은 정답지 20건을 만들고, 파서를 고칠 때마다 대조한다. AI는 표본을 고르고 원문 위치를 안내하는 데까지만 관여하고 값은 사람이 읽는다. 표본은 시기·문서 유형·업종·서식 축을 섞어 층화한다. 항목마다 값과 함께 연결·별도, 회계연도, 기간 유형(3개월·누적), 단위를 적는다.
이유: 지금은 전수 통계만으로 판단하는데, 그 통계가 우리가 만든 규칙으로 뽑은 값을 우리가 만든 규칙으로 센 것이라 자기 참조다. W5에서 `fsdoc.py`를 다섯 번 고치면서 매번 "맞아 보인다"로 넘어갔고, 정답지가 있었으면 즉시 판정됐다. 회귀도 잡히지 않는다. 규칙을 고쳐 A가 나아지고 B가 깨지는 일을 통계로는 못 본다. AI가 값까지 뽑아 사람이 확인하는 방식을 쓰지 않는 이유는, 화면에 숫자가 이미 적혀 있으면 사람이 검증 없이 넘기기 쉽고 틀린 값이 정답으로 굳으면 정답지가 오히려 해가 되기 때문이다.
기각한 대안: 전수 통계로만 판단(자기 참조), AI가 뽑은 값을 사람이 검수(확증 편향), 골든셋 없이 진행(두세 시간을 아끼는 대신 매 수정마다 판정 근거가 없어짐).

### 2026-08-17 데이터 정책을 POLICY.md 로 분리한다
결정: 값을 다루는 원칙을 `docs/POLICY.md`에 모은다. 원문에 없는 값, 원문의 값 오류, 못 뽑은 값, 값의 출처 표기 네 가지다. 정책은 미리 다 세우지 않고, 같은 유형의 판단이 두 번째 나왔을 때 올리며 세 번째부터는 다시 묻지 않는다. `DECISIONS.md`는 시간순 기록이라 지금 유효한 원칙을 한눈에 볼 수 없으므로 나눈다. 근거는 `DECISIONS.md`를 가리킨다.
이유: 같은 성격의 판단이 세 번 반복됐다. "원문에 없는 값을 만들 것인가"가 계약 공시 원문 오류, 소급재작성, 뺄셈 자본총계에서 나왔는데 매번 처음부터 따졌다. 정책이 있으면 개별 사례가 한 줄로 결정되고 판단의 일관성이 생긴다. 사용자가 매 사례마다 결정을 내려야 하는 부담도 준다.
기각한 대안: DECISIONS.md 에만 기록(시간순이라 유효한 원칙을 찾기 어려움), 정책을 미리 전부 수립(무슨 일이 일어날지 모르는 상태에서 정하면 존재하지 않는 것을 전제하게 됨).

### 2026-08-18 D3 업종에 따라 다른 손익 계정을 하나로 묶지 않고 각각 담는다
결정: 제조업의 매출액과 금융업의 순이자손익·순수수료손익을 같은 항목으로 묶지 않는다. `fact_financial`에 업종별로 다른 `item_code`로 담고 전부 원문 값만 쓴다. 비교 질의가 오면 값을 주기 전에 "금융업은 매출액 계정을 쓰지 않아 직접 비교가 어렵다"를 먼저 밝히고, 그다음 각 업종의 값을 구분해 제시한다.
이유: 매출액과 순이자손익은 같은 것의 다른 이름이 아니라 정의상 다른 계정이다. 회계 기준이 금융회사에 순액 표시를 허용하고 있어 손익계산서 최상단이 순이자손익으로 시작하며, KB금융 연결 포괄손익계산서에는 영업수익 소계가 없고 "영업"으로 시작하는 첫 계정이 영업이익이다. K-IFRS상 금융회사의 영업수익 개념(이자수익 + 수수료수익 + 유가증권 평가·처분이익 등)은 존재하나 표시가 강제되지 않아 원문에 나타나지 않는다. 그것을 우리가 합산해 만들면 어느 항목까지 더할지를 우리가 정하게 되므로 POLICY 1항의 "유도가 명시적인 경우"에 해당하지 않는다. 그리고 일부 금융사 공시가 영업이익을 매출액과 같은 뜻으로 표현하기도 해 업계 내에서도 대응이 일정하지 않다. 억지로 한 자리에 맞추면 "삼성전자와 KB금융의 매출 비교"에 근거 없는 답을 주게 된다.
기각한 대안: 이자수익 등을 합산해 영업수익을 만듦(원문에 없는 값), 순이자손익을 매출액 자리에 넣음(성격이 달라 제조업과 나란히 놓으면 오해), 금융업은 손익 항목을 통째로 비움(원문에 있는 값을 버리게 됨).

### 2026-08-18 반기·분기 손익은 3개월치와 누적을 모두 담고 기간유형으로 구분한다
결정: `fact_financial`에 `period_type`(annual · cumulative · quarter)과 `fiscal_period`(Q1 · H1 · Q3 · FY)를 두어 같은 계정의 3개월치와 누적을 나란히 담는다. 어느 하나를 기본으로 두고 나머지를 버리지 않는다. 질의가 어느 쪽인지 모호하면 둘 다 제시하고 무엇을 본 것인지 밝힌다.
이유: 반기보고서 손익계산서는 열이 넷이고(당기 3개월 · 당기 누적 · 전기 3개월 · 전기 누적) 둘 다 원문 값이므로 버릴 이유가 없다. 사업보고서에는 3개월 열이 아예 없어 3개월치만 담기로 하면 4분기 값을 `연간 − 3분기 누적`으로 계산해야 하는데 그것은 원문에 없는 값이라 POLICY 1항에 걸린다. 반대로 누적만 담아도 3개월치는 뺄셈이 필요하다. 둘 다 원문에 있는데 한쪽을 계산으로 만들 이유가 없다. W4에서 계약금액을 `amount_krw` · `amount_fixed` · `amount_cond` 세 컬럼으로 모두 두고 구분해 제시하기로 한 것과 같은 성질의 결정이다. XBRL ACONTEXT가 A(누적)와 Q(분기)를 이미 구분하고 있어 태그가 있는 문서에서는 판정 없이 받을 수 있다는 것도 근거다.
기각한 대안: 누적만 담음(3개월치를 뺄셈으로 만들게 됨), 3개월치만 담음(4분기를 계산해야 하고 원문에 없는 값이 됨). 후자는 담는 행 수가 적고 S1에서 기간을 가르는 판단이 없어 단순하다는 이점이 있으나, 원문 값을 버리는 대가가 크다고 봤다.
발견 경위: 골든 데이터셋 대조에서 드러났다. 삼성전자 2023 반기 매출을 파서는 60,005,533(2분기 3개월), 사람은 123,750,904(상반기 누적)으로 읽었다. 1분기 보고서는 두 값이 같아 우연히 일치했고 반기·3분기에서만 어긋난다.

### 2026-08-18 재무 수치는 최종 정정본 값을 담는다
결정: 같은 회계연도·같은 보고서에 원본과 정정본이 모두 있으면 최종 정정본의 값을 `fact_financial`에 담는다. 원본 값은 담지 않는다. 정정이 있었다는 사실은 `doc_relation`이 이미 갖고 있으므로 값 테이블에서 다시 표현하지 않는다. 골든 데이터셋도 정정본을 대상으로 삼는다.
이유: 질의에 답할 때 써야 하는 값이 정정본이다. 원본 값을 답하면 틀린 답이 된다. 정답지가 답변과 다른 판본을 보고 있으면 검증의 의미가 흐려진다. 그리고 정정본은 앞에 정정 전후 대조표가 붙어 표 구조가 원본과 다르므로, 파서가 그것을 재무제표로 오인하지 않는지 확인할 필요가 있다. 골든은 어려운 쪽을 덮는 편이 낫다. 2026-08-15에 "값 자체의 판본 선택은 W4·W5에서 별도로 정한다"로 미뤄둔 사안을 여기서 정한다.
기각한 대안: 원본을 담고 정정본을 별도 컬럼으로(원본 값은 이미 틀린 값이라 담을 이유가 없고, 정정 사실은 `doc_relation`에 있다), 둘 다 담고 판본 표시(3개월치와 누적처럼 둘 다 유효한 값이 아니라 하나가 틀린 값이라 성질이 다르다).
확인한 사실: 삼성생명 2025년 사업보고서는 원본(2026-03-11)과 기재정정본(2026-03-31)의 재무제표 여섯 값이 전부 같았다. 이번 건은 재무제표를 고치지 않은 정정이었으나 정책은 판본이 갈리는 경우를 위해 정한다.

### 2026-08-18 D3 을 세 갈래로 넓힌다 — 보험업은 은행지주와도 다른 계정 체계다
결정: 2026-08-18 "D3 업종에 따라 다른 손익 계정을 하나로 묶지 않고 각각 담는다"를 두 갈래에서 세 갈래로 넓힌다. 제조·서비스는 `revenue`, 은행지주는 `net_interest_income`·`net_fee_income`, 보험은 `insurance_result`로 담는다. 골든 데이터셋에 삼성생명·삼성화재해상보험 2건 12행을 추가해 보험 계열도 검증 대상에 넣는다.
이유: 코퍼스 70개 기업의 최신 사업보고서에서 연결 손익계산서 첫 계정을 전수로 세니 제조·서비스 63 · 은행지주 5 · 보험 2로 갈렸다. 보험사는 삼성생명이 `보험서비스결과`, 삼성화재해상보험이 `보험손익`으로 순이자손익도 매출액도 아니다. IFRS 17 보험계약 기준을 적용하면서 보험료 수취 시점이 아니라 서비스 제공 기간에 나눠 수익을 인식하게 됐고, 그 결과 은행의 이자 구조와도 다른 계정이 생겼다. 70개 중 2개뿐이나 코퍼스에 있는 이상 평가 질의에 나올 수 있고, 검증할 정답지 없이 담으면 틀려도 모른다.
함께 확인한 것: `영업수익`은 통신·운송·서비스 업종이 쓰는데 `ifrs-full_Revenue` 태그를 단다. 회계 기준이 매출액과 같은 것으로 보므로 revenue 계열에 넣는다. 그리고 신한지주·우리금융지주·메리츠금융지주는 손익계산서 첫 계정이 `영업이익`이고 그 아래에 순이자손익이 들여쓰기로 붙는 계층 구조다. 첫 행을 수익으로 보는 방식이었다면 영업이익을 매출로 담았을 것이다.

### 2026-08-21 원문 XML 을 파싱 전에 sanitize 로 교정한다
결정: DART 원문의 이스케이프 누락 세 가지를 파싱 전에 바로잡는 `src/sanitize.py` 를 두고 본문 계층 경로에서 쓴다. (1) 본문 텍스트의 꺾쇠를 `&lt;` 로, (2) 속성 값 안의 따옴표를 `&quot;` 로, (3) XML 에 없는 엔티티를 뜻하던 문자로 바꾼다. 표준 태그 33종은 전수 집계로 확정했고 그 밖의 `<...>` 는 전부 텍스트로 본다.
이유: 원문 자체는 멀쩡하다. 1,051건 전수에서 `<TABLE>` 과 `</TABLE>` 개수가 전부 맞았다. 이스케이프만 빠져 있고 XML 규칙상 그것이 태그 시작으로 읽힌다. 가짜 태그가 하나 열리면 닫는 태그가 한 칸씩 밀려 `</TABLE>` 이 `<TR>` 을 닫는 데 쓰이고, `<TABLE>` 이 열린 채 남아 그 뒤 문서 전체를 삼킨다. 문서 468건(44.5%)에서 이 일이 벌어졌고 TITLE 30,426개가 제 자리를 잃었다. 속성 값 따옴표는 파서가 그 태그를 통째로 버리게 만들어 같은 밀림을 낳으며, 손실이 문서당 31~68%로 더 크다. 셋을 고치니 파서 오류 1,008건 → 0, 깨진 표 468건 → 0, 글자 손실 0.1% 초과 84건 → 0 이 됐다. 문자열 자체를 대조해도 표본 16건에서 한 글자도 다르지 않았다.
기각한 대안: 파싱 결과에서 깨진 표를 걸러내는 후처리(`section.py` 의 `_broken()`). 속성 값 따옴표 문제는 파싱 단계에서 태그가 사라진 것이라 결과에 그 내용이 아예 없다. 없는 것은 되살릴 수 없다. 파서를 HTML 모드로 바꾸는 방안도 검토했으나 DART 서식은 HTML 이 아니라 태그 의미가 달라진다.
대가: 원문 90.6M자에 52.8초가 든다. 파싱(8.2초)보다 6배 무겁고 전수 1,051건이면 약 58분이 추가된다. 자리표시자를 쓰지 않는 가벼운 방식을 시도했다가 정확성(표본 16건 중 11건 불일치)과 속도 둘 다 나빠 되돌렸다.
남긴 것: `corpus.read_raw()` 에는 넣지 않았다. 넣으면 재무 추출 경로도 바뀌어 `fact_financial` 38,845행과 골든 129건을 다시 검증해야 한다. 두 경로가 서로 다른 원문을 보게 되는 것이 대가이며, 재무를 sanitize 원문으로 다시 뽑아 값이 달라지는지 대조한 뒤에 정한다.

### 2026-08-21 원문 XML 을 파일별로 따로 파싱한다
결정: 문서 폴더에 XML 이 여럿이면 이어붙이지 않고 파일마다 따로 파싱한 뒤 조각을 잇는다. 첨부 감사보고서는 같은 `doc_id` 아래 seq 를 이어 담는다.
이유: `corpus.read_raw()` 가 폴더의 XML 을 문자열로 이어붙인다. 사업보고서는 본문·감사보고서·연결감사보고서 세 파일이라 이어붙이면 `<?xml ?>` 선언이 문서 중간에 오고 루트가 셋이 된다. XML 은 루트가 하나여야 하므로 파서가 첫 루트만 읽고 나머지를 버린다. 사업보고서 210건에서 감사보고서 내용이 통째로 빠져 목차 제목만 0자로 남아 있었다. 감사의견과 핵심감사사항은 질의 대상이 될 가능성이 높다.
기각한 대안: 별도 문서로 등록(`document` 에 415행 추가). 목차 체계가 섞이지 않는 이점이 있으나 rcept_no 와 doc_subtype 을 새로 정해야 하고, 본문과 첨부의 관계를 잇는 테이블이 필요해지며, 기존 doc_id 기반 조회가 감사보고서를 놓친다. 담지 않는 방안도 기각했다. 목차 제목만 있고 내용이 빈 조각이 남아 검색이 그것을 물면 근거를 붙일 수 없다.
되돌릴 수 있는 선택이다: section 을 다시 만들면 바뀐다. 나중에 별도 문서로 옮기려면 그때 결정한다.

### 2026-08-21 BM25 토큰화에 회계 용어 사전과 접두사 보존 규칙을 넣는다
결정: `src/terms.py` 에 회계·공시 용어 사전 12,750개를 두고 `search.tokenize()` 에서 세 가지를 한다. (1) 사전을 kiwipiepy 사용자 사전으로 등록해 복합 용어가 하나로 잡히게 한다. (2) 체언 접두사(XPN)를 버리지 않고 앞 명사에 붙인다. (3) 사전 낱말은 원형과 조각을 둘 다 토큰으로 낸다.
이유: 검증 3에서 형태소 분석기가 회계 용어를 쪼갠다는 것이 드러났다. 실측하니 문제가 두 갈래였고 해법이 달랐다. 삼성전자 조각 2,331개에서 상위 10개 중 질의 낱말이 원문에 실제로 든 것의 수를 재니, `미지급비용` 5/10 · `자산총계` 3/10 · `부채총계` 9/10 이었다. `미지급비용` 은 '미'가 접두사라 버려져 '지급비용'이 되고 임원 보수 조각이 함께 걸린 것으로, 접두사 보존 규칙만으로 10/10 이 됐다. `자산총계` 는 '자산'과 '총계'가 둘 다 흔해 부문별 보고 조각이 상위를 차지한 것으로, 규칙으로는 안 잡히고 사전이 필요했다. 사전을 넣으니 10/10 이 됐다. 우려했던 `매출채권` 은 어느 방식에서도 10/10 으로 문제가 없었다. 최종 방식에서 나빠진 항목은 하나도 없고 토큰이 1.10배가 됐다.
기각한 대안: 선별 지표로 사전 후보를 고르는 방식. 독립비율(조각이 복합어 밖에서 쓰이는 비율)과 혼동도(조각이 함께 나타나되 원어는 없는 조각의 비율)를 각각 전수로 재봤으나 실측 순서를 재현하지 못했다. 독립비율은 자산총계를 네 번째로, 혼동도는 영업이익을 자산총계보다 위험하다고 판정했다. BM25 점수가 IDF·TF·문서 길이가 얽힌 값이라 단일 지표로 근사가 안 된다. 사전에 넣어 나빠진 사례를 하나도 못 찾았으므로 선별을 포기하고 넣을 자격만 보기로 했다. 사전만 넣고 조각을 안 내는 방식(D)도 이번 표본에서는 같은 결과였으나, 부분 질의에 대비해 합집합을 택했다.
사전을 만드는 규칙: 표 행 머리에서 뽑고, 전수 20회 이상, 길이 3~14자, 명사로만 이루어진 것('및'·'와'로 이은 계정명은 예외), 실제로 쪼개지는 것만 넣는다. `lookup.py` 의 LABEL·KEYWORD 는 무조건 넣는다. 연속 숫자 4자리 이상, 숫자로 시작하는 7자 이하, 8자 미만인데 숫자가 든 것, 표 서식 낱말은 뺀다. 기간 표시어(당분기·전기말)는 빼지 않는다. 빼면 '당분'(설탕)처럼 잘못 잘린 채로 남기 때문이다.
남긴 것: 사전이 이 코퍼스의 표 행 머리에서 나온 것이라 본문 문장 속 용어는 덜 잡힌다. '핵심감사사항'처럼 표가 아니라 문장에 나오는 말이 그렇다. 그리고 이번 판정에 쓴 질의는 AI 가 만든 것이라, 사용자가 만들 질의 세트로 다시 재야 최종 확인이 된다. 되돌리는 비용은 토큰 재생성 133분이고 돈이 들지 않는다.

### 2026-08-21 부분 토큰에서 뺄 조각을 두 규칙으로 거른다
결정: 사전 낱말의 조각을 토큰으로 낼 때 두 조건에 걸리는 것을 뺀다. (1) 그 조각이 든 조각이 전체의 40% 이상이면 뺀다. (2) 조각의 문서빈도를 그것을 쓰는 용어의 문서빈도로 나눈 값이 1.05 미만이면 뺀다. 조각 3,755개 중 121개(3.2%)가 걸린다.
이유: `review_top.csv` 를 검토하니 쓸모없는 조각이 섞여 있었다. `당분기말` 이 `당분`(설탕) · `기말` 로, `전분기` 가 `전분`(녹말) 로 쪼개졌고, `기업명` 의 조각 `기업` 은 전체 조각의 49%에 나와 변별력이 없었다. 조각별 문서빈도를 전수로 재니 두 신호가 서로 다른 것을 잡았다. 당분(원어대비 1.02) · 전분(1.01)은 원어 안에서만 나타나는 조각이고, 기업(49.0%) · 기타(55.5%) · 기말(44.2%) · 단위(74.4%)는 어디에나 있는 조각이다. 유지해야 할 순이익(12.4% · 1.57) · 잉여금(8.0% · 1.18) · 총계(7.2% · 1.74) · 채권(27.1% · 2.04)은 두 조건 어디에도 안 걸린다. 필터 적용 전후로 BM25 를 돌려 12개 질의에서 나빠진 항목이 없음을 확인했고 토큰은 3.9% 줄었다.
기각한 대안: 더 느슨한 조합(35% · 1.10). `재고`(1.07)와 `포괄`(1.07)까지 빠지는데, `재고` 는 단독 질의에 쓰일 만해서 남겼다. 임계값을 아예 두지 않는 방안도 있었으나, 지적받은 사례가 그대로 남는다.
남긴 것: `흐름`(1.04)과 `무형`(1.02)이 빠진다. 원형인 `현금흐름` · `무형자산` 이 사전에 있어 그 형태로는 걸리지만, `흐름` 이나 `무형` 단독 질의는 BM25 로 못 잡는다. 그런 질의가 실제로 올지는 질의 세트를 봐야 안다.

### 2026-08-21 임베딩을 10개 기업씩 7 묶음으로 나눈다
결정: `data/eval/batches.csv` 에 배정을 두고 순차로 임베딩한다. 1 묶음은 실측으로 문제가 몰렸던 기업으로 채우고, 2~7 묶음은 조각 수를 고르게 맞추고 업종을 섞는다. 각 묶음이 끝날 때마다 질의로 확인하고 다음으로 간다.
이유: 임베딩은 되돌릴 수 없고 53,785원이 든다(조각이 141,524에서 171,564로 늘어 1차 시도의 42,474원보다 커졌다). 전수로 하다 문제를 발견하면 전부 버리지만, 나누면 손실이 한 묶음 약 7,000~10,000원으로 제한된다. 1 묶음에 어려운 기업을 모으는 것은 문제가 있다면 첫 묶음에서 만나야 하기 때문이다. KB금융(속성 오류 29,553개 · 조각 최다 · 금융지주 · PDF 대체 수집분) · 삼성생명(보험 IFRS17) · 신한지주(한 절이 98.1%) · 현대자동차(밀림이 DOCUMENT 까지) · CJ제일제당(손실 67.9%) · NC(중첩 표 57개) · 크래프톤(한 절이 99.6%) · HD현대일렉트릭(손실 55.8%) · NAVER(손실 31.5%) · 시프트업(조각 731개로 최소)을 넣었다. 계정 체계 세 갈래(제조·은행지주·보험)가 다 들어 있다. 2~7 묶음을 조각 수로 맞춘 것은 기업 규모가 9.6배까지 벌어져(KB금융 7,032 · 시프트업 731) 기업 수로만 나누면 비용과 시간이 예측되지 않기 때문이다. 결과가 23,256~23,748로 2% 안에 들어왔다.
기각한 대안: 질의를 먼저 만들고 거기 나온 기업으로 1 묶음을 구성하는 방식. 실제로 물어볼 것이 먼저 정해진다는 이점이 있으나, 질의를 만들려면 데이터가 있어야 하는 순환이 생긴다.
질의 작성: 임베딩 점검용이므로 AI 가 만든다. 조각에서 질의를 만들고 그 조각이 상위에 오는지 보는 방식이라 정답을 조각이 정한다. 검색 품질 평가는 성격이 다르므로 별도로 한다.

### 2026-08-22 검색 측정 코드를 src/retrieval.py 한 곳으로 모은다
결정: 검색 실행과 채점을 `src/retrieval.py` 에 모으고, 평가 진입점을 `scripts/eval_body.py` 하나로 둔다. 사례와 정답은 `data/eval/body_cases.csv` 에 둔다.
이유: 평가 스크립트를 셋 만들었더니 서로 다른 숫자를 냈다. 같은 질의에 `eval_evalset` 은 문항 16·20 적중에 자체제작 5가 4위였고 `tune_stop` 은 셋 다 1위였다. 후보 구성과 채점 기준이 조금씩 달랐고 어느 것이 맞는지 알 수 없었다. 더 나쁜 것은 `tune_stop` 이 STOP 설정을 어떻게 바꿔도 3/8을 냈다는 것이다. 사용자가 "코드가 꼬여서 예전 결과를 반복하거나 동일한 설정으로 검증하고 있는 것"이라고 지적했고 실제로 그랬다. `query.py` 가 "보고서" 를 정규식과 STOP 두 곳에서 지우고 있어 STOP 을 바꿔도 효과가 없었다. 측정 코드를 못 믿으면 무엇을 고쳐도 확인이 안 된다.
함께 고친 것: `목 차` 조각을 검색 후보에서 뺐다. 문서의 절 제목을 나열한 조각이라 어떤 질의와도 낱말이 겹쳐 벡터 유사도가 높게 나온다. SK하이닉스 질의에서 벡터 1위(0.7274)를 차지해 정답(0.6432)을 11위로 밀어냈다. 이것을 빼기 전에는 벡터가 BM25보다 나빠 보였는데, 빼고 나니 뒤집혔다.

### 2026-08-22 RRF 가중치를 1:1 로 두되 확정하지 않는다
결정: 벡터와 BM25 의 RRF 가중치를 1:1 로 쓴다. `scripts/eval_body.py` 의 `W_DEFAULT` 에 두고 언제든 바꾼다. 확정하지 않는다.
이유: 사례 수에 따라 최적값이 뒤집혔다. 9개로 잴 때는 2:1 이 8위내 7/9 로 가장 나았는데, 37개로 늘리니 1:1 이 29/37 로 가장 낫고 2:1 은 27/37 이었다. 한두 건에 순위가 바뀌는 규모라 지금 값을 코드에 박으면 그 숫자가 나중 판단을 흐린다.
37건 실측: 1:0 (벡터만) 1위 9 · 8위내 20 / 0:1 (BM25만) 15 · 25 / 1:1 12 · 29 / 1:2 16 · 28 / 1:5 18 · 25 / 2:1 13 · 27. 1위 적중은 BM25 쪽에 무게를 줄수록 오르고 8위내는 균형이 낫다. 생성 모델에 여러 조각을 넣으므로 8위내를 우선했다.
기각한 대안: 벡터를 빼고 BM25 만 쓰는 방안. 8위내가 25/37 로 1:1 보다 낮다. 벡터를 빼면 낱말이 안 겹치는 질의를 통째로 놓친다.

### 2026-08-22 질의 해석기(S1)를 규칙으로 만든다
결정: `src/query.py` 에서 질의를 필터·의도·검색어로 가른다. 기업·업종·시장·연도·날짜·보고서 종류를 규칙으로 뽑고, 질의 동사와 연결어를 검색어에서 뺀다. 생성 모델을 쓰지 않는다.
이유: 평가 질의 28개로 재보니 규칙만으로 27개에서 대상을 뽑아냈다. 기업명이 없는 질의도 업종이나 시장으로 걸린다("전력기기 산업" → sector, "코스닥 기업들" → market). 규칙은 빠르고 재현되며 비용이 없다. 생성 모델을 부르면 질의마다 호출이 하나 늘고 같은 질의에 다른 답이 나올 수 있다.
STOP 목록의 효과: 37건 기준으로 갈래를 어떻게 바꿔도 8위내가 29~30건으로 거의 같았다. 검색어가 평균 5.4개에서 3.7개로 줄어도 성적이 유지되므로 뺀 낱말들이 도움이 안 됐다는 뜻이다. 다만 개선도 아니다. 검색어를 줄이면 후보 축소가 빨라지므로 유지한다.
겪은 실패: 처음에 STOP 을 과도하게 걸어 "SK하이닉스가 어떤 사업을 하는 회사인지 사업보고서 기준으로" 의 검색어가 ['사업','회사'] 둘만 남았다. 정답 순위가 17위에서 125위로 떨어졌다. "보고서" 와 "기준" 이 정답 조각에도 들어 있어 단서 노릇을 하고 있었다. 잡음을 줄이려다 단서를 없앤 것이다.
남긴 것: 용어 확장은 안 만들었다. "실적" → "손익계산서", "전속 연예인" → "전속계약금" → "무형자산" 같은 연결이다. 규칙으로는 안 되고 사전이나 생성 모델이 필요하다. 37건 중 실패한 것 상당수가 여기 걸린다.

### 2026-08-22 경로 매핑을 평가 질의가 아니라 절 본문에서 뽑는다
결정: 절마다 대표 낱말을 뽑을 때 평가 질의를 참고하지 않고 절의 본문에서만 뽑는다. 점수는 `(그 절에 나오는 비율) ÷ (전체 절에 나오는 비율)` 로 낸다. 결과는 `data/eval/pathmap.csv`, 생성은 `scripts/build_pathmap.py`.
이유: 평가 질의를 보고 만들면 그 질의에만 맞춰진다. 심사에서 다른 것을 물으면 안 잡힌다. 절 본문에서 뽑으면 질의 표현이 달라도 같은 절을 짚는다.
기각한 대안: 평가 질의와 정답 절을 직접 이어 매핑을 만드는 방식.

### 2026-08-22 경로 매칭을 낱말 개수가 아니라 점수 합으로 판정한다
결정: 질의 낱말이 절의 대표 낱말과 몇 개 겹치는지 세지 않고, 겹친 낱말의 점수를 더해 문턱(`PATH_MIN = 15.0`)을 넘을 때만 경로를 짚는다.
이유: 낱말마다 변별력이 다르다. `배당성향` 은 III/6 을 특정하지만 `정책` 은 아무 절이나 가리킨다. 개수로 세니 "주주환원 정책" 이 `정책` 하나 때문에 IV/6 을 짚어 성적이 12/37 에서 10/37 로 떨어졌다. 점수로 바꾸니 14/37 이 됐다.
기각한 대안: 겹친 낱말 개수 기준, 매칭 없이 일반 검색만 쓰는 방식.

### 2026-08-22 경로를 짚어도 후보를 좁히지 않고 순위만 올린다
결정: 짚은 경로의 조각을 앞으로 당기되(`PATH_BOOST = 20`) 나머지를 후보에서 빼지 않는다.
이유: 잘못 짚으면 정답을 통째로 버린다. `II/1 사업의 개요` 처럼 대표 낱말이 안 잡히는 절이 있어 오탐을 완전히 없앨 수 없다. 순위만 올리면 잘못 짚어도 정답이 후보에 남는다.
기각한 대안: 짚은 경로만 후보로 남기는 방식(후보가 300조각에서 5조각으로 줄어 검색이 거의 필요 없어지지만, 오탐 시 회복이 불가능하다).

### 2026-08-22 검색 채점을 조각이 아니라 절 단위로 한다
결정: 정답 절의 어느 조각이 걸리든 맞은 것으로 센다. 정답 절 목록은 사용자가 만든 `EVALSET_SOURCE.md` 에서 옮긴다.
이유: 조각은 찾기 위한 단위이고 답을 주는 단위는 절이다. 생성 모델에는 절을 넘긴다. 조각 3번이 아니라 5번이 걸렸다고 틀렸다고 하면 실제 성능보다 낮게 나온다. 정답을 사람이 정해 AI 가 자기 채점 기준을 만들지 않게 한다.
기각한 대안: 조각 단위 채점, AI 가 정답 절을 판정하는 방식.

### 2026-08-22 목차와 확인서 조각을 검색 후보에서 뺀다
결정: `목 차` · `【대표이사 등의 확인】` · `전문가의 확인` 등을 후보에서 제외한다. `retrieval.SKIP_TITLE` 이 판정한다.
이유: 목차는 그 문서의 절 제목을 전부 나열한 조각이라 어떤 질의와도 낱말이 겹친다. SK하이닉스 질의에서 벡터 1위(0.7274)를 차지해 정답(0.6432)을 11위로 밀어냈다. 찾아 줘도 답이 안 나오는 조각이다. 빼기 전에는 BM25 가 벡터보다 낫다고 판정하고 있었는데 빼고 다시 재니 결론이 뒤집혔다.
기각한 대안: 후보에 두고 점수만 낮추는 방식.

### 2026-08-30 세션 인계를 docs/HANDOFF.md 한 파일로 한다
결정: 대화 세션이 바뀔 때 필요한 것을 `docs/HANDOFF.md` 에 모은다. 실측 행 수, 다시 만드는 비용, 확정하지 않은 값과 그 위치, 미결 위험, 다시 밟지 말 함정, 자주 쓰는 명령이 들어간다. 프로젝트 `CLAUDE.md` 의 세션 시작 순서에서 1번으로 둔다.
이유: `PLAN.md` 는 W 단위의 거친 상태만 담아 "가중치를 왜 1:1 로 뒀고 어디를 고치면 되는가" 같은 것이 안 들어간다. `feedback/W*.md` 는 경위를 담아 길다. 세션이 끊긴 뒤 재개하려면 지금 상태를 한 곳에서 봐야 한다.
기각한 대안: PLAN.md 에 세부를 덧붙이는 방식, 세션마다 대화 요약을 남기는 방식.

### 2026-08-31 폐기하기로 한 평가 스크립트 셋을 실제로 지운다
결정: `scripts/eval_evalset.py` · `tune_stop.py` · `eval_retrieval.py` 를 삭제한다. 지우기 전에 `eval_retrieval.py` 의 `--qfile` 을 `eval_body.py` 의 `--cases=` 로 흡수했다. 셋은 `f3af3b6` 커밋에 있고 `git show f3af3b6:scripts/eval_evalset.py` 로 꺼낸다.
이유: 08-22 에 "평가 진입점을 eval_body.py 하나로 둔다" 고 결정하고도 파일을 안 지웠다. 결정이 문서에만 있고 저장소에는 없으면 다음 사람은 저장소를 먼저 본다. 게다가 셋은 단순 중복이 아니라 지금 돌리면 틀린 숫자를 내는 코드였다. 셋 다 `retrieval.py` 를 안 쓰고 후보 구성을 자체 구현해서 목차 조각을 후보에서 빼지 않는다. 목차 조각을 빼기 전에는 BM25 가 벡터보다 낫다고 판정하고 있었고 빼고 나니 결론이 뒤집혔다. `eval_evalset` 과 `tune_stop` 은 질의를 코드에 박아 둬 사례가 8~9건이고 정본은 37건이다.
기각한 대안: 첫 줄에 `SystemExit` 을 걸어 실행만 막고 파일은 남기는 방식. 남길 이유가 "나중에 왜 지웠는지 확인" 하나뿐인데 git 이 이미 그 일을 한다. 죽은 코드가 남으면 새로 온 사람이 넷 중 무엇이 진짜인지 매번 판단해야 하고 일괄 수정 때 같이 손대게 된다. 파일 상단에 폐기 표시만 다는 방식도 기각했다. 표시를 안 읽으면 그냥 실행된다.

### 2026-08-31 인계 문서 머리에 스스로를 의심하라는 지시를 둔다
결정: `docs/HANDOFF.md` 첫머리에 "이 문서를 믿기 전에 대조하라" 를 적고, 08-30 판에서 나온 어긋남 셋을 실례로 남긴다. 세션 시작 프롬프트에도 대조 지시를 넣는다.
이유: 08-30 판을 새 세션이 검증하니 어긋남이 셋 나왔다. DB 파일 이름이 `disclosure.db` 로 잘못 적혔고(실제 `corpus.db`), 테이블 12개 중 8개만 실렸고(가장 큰 `holding_item` 153만 행이 누락), 폐기 대상 스크립트가 남아 있다는 사실이 안 적혔다. 인계 문서는 다음 세션이 가장 먼저 읽고 가장 강하게 믿는 문서라 틀리면 그 위의 판단이 전부 틀어진다. 검증을 문서 안에 지시로 넣어야 매번 실행된다.
기각한 대안: 어긋남을 정정표로 따로 만들어 전달하는 방식. 그러면 다음 세션이 문서와 정정표 둘을 읽어야 하고, 정정표를 놓치면 원래 오류를 그대로 밟는다.

### 2026-08-31 평가 표본의 출처를 문서에 구분해 적고 종합 검토 시점을 정답지 수령 시로 둔다
결정: `docs/HANDOFF.md` §9 를 "사용자가 만든 것" 과 "AI 가 만든 것" 으로 가른다. `body_cases.csv` 37건 중 9건만 사용자 유래이고 28건은 AI 가 질의와 정답을 모두 만든 것임을 명시한다. `W_DEFAULT` 1:1 과 `PATH_MIN` 15.0 이 그 표본 위에서 정해졌다는 사실을 §4 에 적고, 재측정 시점을 사용자가 평가 질의 28개의 정답지를 넘기는 때로 둔다. 그 전에 먼저 정답지를 요청하지 않는다.
이유: `data/eval/README.md` 가 "AI 가 질의를 만들면 답이 있는 것만 묻고, 구현한 구조에 맞춰 묻고, 약한 곳을 피해 간다" 고 적어 뒀는데, 정작 채점용 사례 37건 중 28건이 그 방식으로 만들어졌다. 문서에는 "위 둘에서 옮긴 채점용 사례 37건" 이라고만 적혀 있어 전부 사용자 유래로 읽힌다. 성능 수치의 신뢰 구간을 다음 세션이 오해하면 그 위의 판단이 전부 틀어진다. 정답지를 미리 받지 않는 것은 시험지를 보고 만든 시스템이 그 시험지에만 맞춰지기 때문이다.
기각한 대안: `body_cases.csv` 에서 AI 가 만든 28건을 빼는 방식. 사례가 9건으로 줄면 한 건에 성적이 11%씩 움직여 방향조차 못 본다. set1 질의는 임베딩 점검용으로는 유효하다.

### 2026-08-31 시점 정보를 코드 주석에 적지 않고 커밋 해시만 남긴다
결정: 코드 주석에는 커밋 해시를 적고 날짜는 `docs/HANDOFF.md` 한 곳에만 둔다. `scripts/eval_body.py` 의 삭제 시점 주석에서 날짜를 뺐다.
이유: 같은 날짜를 두 곳에 적었더니 하루 어긋났다. `eval_body.py` 는 08-30, `HANDOFF.md` 는 08-31 로 적혔고 실제 커밋은 08-31 이었다. 커밋 해시는 안 바뀌지만 날짜는 작업일과 커밋일이 갈릴 때 어긋난다. 같은 처리를 두 곳에서 하지 않는다는 원칙의 문서판이다.
기각한 대안: 양쪽 날짜를 맞추고 그대로 두는 방식. 다음에 또 어긋난다.

### 2026-08-31 D2 설비투자를 연간 총액으로 정한다
결정: 설비투자를 묻는 질의에 현금흐름표의 유형자산 취득액(`fact_financial` 의 `capex`, XBRL 태그 `ifrs-full_PurchaseOfPropertyPlantAndEquipmentClassifiedAsInvestingActivities`) 연간 총액으로 답한다. 거래소 신규시설투자 공시의 개별 건은 기본 답이 아니다. "일단" 이라는 단서가 붙은 결정이다.
이유: 덮는 범위가 다르다. 연간 총액은 사업보고서 25건 표본 전부에 태그로 있어 70개사 전 기간이 고르게 덮인다. 개별 공시는 43건 21개사뿐이라 나머지 49개사에는 답이 아예 없다. 그리고 참고 질의에 "A와 B 중 2025년 설비투자 규모가 더 큰 기업은 어디인가" 라는 규모 비교형이 직접 나오는데, 이는 총액이라야 답이 된다.
기각한 대안: 질의 형태로 갈라 총액과 개별 공시를 라우팅하는 방식. 답의 정확도는 이쪽이 높으나 S1 에 분기가 하나 늘고 그 분기가 틀리면 답이 아예 안 나간다. 남은 일정에서 그 분기를 검증할 여유가 없다고 봤다. 개별 공시를 기본으로 두는 방식도 기각했다. 커버리지가 21개사로 좁다.
남긴 것: 개별 공시 43건은 `event_major` 에 그대로 있다. 총액으로 답하면서 "개별 투자 건은 따로 있다" 를 안내하는 방식으로 나중에 넓힐 수 있다.

### 2026-08-31 D5 의 "최근" 을 3개년으로 해석하고 그 해석을 답변에 반드시 밝힌다
결정: 기간을 명시하지 않은 "최근" 질의는 3개년으로 해석한다. 그리고 답변에 "최근을 3개년으로 해석했다" 를 반드시 적는다. 해석을 밝히는 것은 선택이 아니라 필수다.
이유: "최근" 에는 정답이 없다. 어떤 값을 골라도 심사자가 다른 기간을 의도했을 수 있다. 그때 해석을 밝혀 두면 답이 왜 그 범위인지 확인되고, 밝히지 않으면 그냥 틀린 답이 된다. 대회 평가지표가 근거 제시와 한계 고지를 항목으로 두고 있어, 해석을 드러내는 것이 감점이 아니라 득점 요인이다. 3개년으로 정한 것은 코퍼스가 2023~2026년을 덮어 3개년이면 대부분의 기업에서 실제로 값이 나오기 때문이다.
기각한 대안: 질의마다 기간을 되묻는 방식. 평가가 GET 요청 한 번으로 끝나므로 되물으면 그 질의의 답이 없다. 기간을 안 정하고 있는 값을 전부 내놓는 방식도 기각했다. 범위를 안 밝히면 무엇을 근거로 고른 것인지 확인이 안 된다.
남은 것: 이 결정은 D5 의 절반만 답한다. `docs/GLOSSARY.md` 의 D5 본문이 묻는 질문은 "'2025년' 이 회계연도인가 접수일 기준인가" 이고 그쪽은 아직 미결이다. FY2025 사업보고서가 2026년 3월에 접수되므로 두 기준이 한 해씩 어긋난다. S1 질의 해석과 S3 문서 선별에 직접 걸린다.

### 2026-08-31 D5 정기공시는 회계연도, 수시공시는 접수일로 기간을 해석한다
결정: 질의의 연도를 정기공시(사업·반기·분기보고서)에서는 회계연도(`document.base_year`)로, 수시공시(거래소 계약·주요사항·지분)에서는 접수일(`rcept_dt`)로 해석한다. 그리고 어느 기준으로 문서를 골랐는지를 답변에 반드시 적는다. 2026-08-31 "D5 의 '최근' 을 3개년으로 해석한다" 와 함께 D5 를 닫는다.
이유: 데이터 구조가 그렇게 생겼다. `base_year`·`base_month` 는 정기공시 1,054건에만 있고 나머지 3,150건은 결측이라 `rcept_dt` 로만 판단할 수 있다. 규칙을 하나로 통일해 전부 접수일로 두면 "2025년 사업보고서" 가 FY2024 보고서를 가리키게 되어 통념과 어긋난다. FY2025 사업보고서는 2026년 3월에 접수되기 때문이다. 기준을 답변에 밝히는 이유는 "최근 3개년" 과 같다. 두 기준이 한 해씩 어긋나므로 어느 쪽을 썼는지 안 밝히면 사용자가 답이 맞는지 확인할 수 없다.
기각한 대안: 전부 접수일로 통일하는 방식. 규칙이 하나라 단순하지만 정기공시에서 통념과 한 해 어긋난다. 전부 회계연도로 통일하는 방식도 기각했다. 수시공시 3,150건에 회계연도가 아예 없다.

### 2026-08-31 sanitize 를 corpus.read_raw() 에 넣는 일(risk A)을 W8 이후로 미룬다
결정: `sanitize` 를 지금은 `build_section.py` 경로에만 두고 `corpus.read_raw()` 에 넣지 않는다. 끝까지 구현이 되는지 먼저 확인하고 나중에 넣는다.
이유: 실측으로 영향 범위를 확정했다. `build_fs.py` 의 선정 로직을 복제해 적재 대상 895건을 만들고 그중 37건을 `extract(raw)` 와 `extract(sanitize(raw))` 로 대조하니 동일 25 · 다름 12 였다. 다른 12건은 전부 "raw 에만 있는 값 0" 이다. 즉 담긴 값이 틀린 것이 아니라 별도(개별) 재무제표가 통째로 빠져 있다. 연결 기준 값은 raw 에서도 정상으로 나왔다. 영향 문서는 HD현대일렉트릭 6 · 하나금융지주 4 · NC 2 로 12건이고 적재 대상 895건의 1.3% 다. 세 기업은 골든 대상 22문서와 겹치지 않고 평가 질의 28개에도 안 나온다. 값이 틀린 것이 아니라 빠진 것이므로 지금 상태에서도 틀린 답이 나가지 않는다. 별도 기준을 물으면 "확인되지 않음" 이 나가고 그것은 평가 항목이다.
번복한 전제: `docs/HANDOFF.md` §6 과 `docs/feedback/W6.md` 가 "골든 129건을 다시 검증해야 한다" 고 적어 둔 것은 틀렸다. 겹치는 문서가 없어 재검증 부담이 없다. 나중에 넣을 때 드는 것은 `build_fs.py --reset` 재실행(공짜)과 그 12건 확인뿐이다.
기각한 대안: 지금 넣는 방식. 얻는 값이 12건의 별도 재무제표뿐인데 W8 이 아직 없다.

### 2026-08-31 CLOVA 임베딩을 전량 수행하되 소요 시간은 측정해서 정한다
결정: `chunk.embedding` 을 171,564 전량 채운다. 다른 작업과 병행한다. 다만 저장소에 적힌 소요 시간 추정 셋이 서로 어긋나므로, 본 실행 전에 표본을 재서 실제 속도를 확정한다.
이유: 병행은 가능하다. `build_embedding.py` 는 HTTP 호출을 순차로 던지고 기다리는 구조라 CPU 를 거의 안 쓴다. `embedding IS NULL` 인 것만 처리하므로 중단해도 이어서 돌고 같은 조각을 두 번 호출하지 않는다. 검색은 `embedding_oa` 로 돌아가므로 W8 개발이 막히지 않는다. 시간을 측정으로 정하는 이유는 저장소의 세 숫자가 4배 넘게 벌어져 있기 때문이다. `docs/PLAN.md` 는 분당 25건 · 94시간, `src/chunk.py` 는 5,000자 한도면 14.5시간, `src/clova.py` 는 300건 실측에서 분당 88건이라고 적혀 있다. 셋 다 전량 실행을 잰 값이 아니다.
정정한 값: PLAN 의 94시간 · 50,265원은 조각이 141,524 이던 시절 값이다. 지금 171,564 조각 · `token_est` 합계 294,653,192 기준으로 비용은 58,931원이다. 시간은 분당 88건이면 32.5시간, 분당 25건이면 114.4시간으로 측정 전에는 범위로만 말할 수 있다.

### 2026-08-31 감사보고서 첨부 415건을 별도 문서로 등록한다 (risk B 방향 A)
결정: 정기공시 폴더의 첨부 감사보고서를 `document` 별도 행으로 떼어낸다. `doc_id` 는 `audit_{첨부파일명}`, `doc_group` 은 `audit` 이다. `src/build_audit_doc.py` 가 적용하고 `scripts/verify_audit_doc.py` 6겹이 검증한다. 실행 결과 문서 415행 추가 · section 4,525행 이동 · chunk 13,861행 이동, 관문 6겹 전부 통과.
이유: 감사의견을 근거로 답할 때 근거 공시가 사업보고서로 표시되던 문제를 없앤다. CLOVA 임베딩보다 먼저 한 이유는 순서를 뒤집으면 재작업이 붙기 때문이다.
정정한 추정: 앞서 "13,861조각 재작업 · CLOVA 기준 6,442원" 이라고 추정했으나 실제 비용은 0원이다. `section.doc_id` 와 `chunk.doc_id` 두 칼럼만 바꾸므로 `chunk_id` 가 그대로이고 `tokens` · `embedding_oa` · `embedding` 이 전부 살아 있다. 본문을 다시 자르지도 않는다. `fact_financial` 도 안 건드린다. `build_fs.py` 는 `section` 이 아니라 원문 파일에서 값을 뽑기 때문이다.
설계에서 조심한 것: `src/retrieval.py` 의 `Corpus` 가 `doc_subtype` 으로 거르고 `base_year` 로 최신 연도를 고른다. 새 문서 행이 이 둘을 부모에서 물려받지 않으면 감사보고서가 검색에서 통째로 사라진다. 그래서 `doc_subtype`·`base_year`·`base_month`·`corp_code`·`corp_name`·`rcept_dt`·`flr_nm`·`is_correction`·`file_path` 를 부모에서 물려받고 `doc_id`·`doc_group`·`report_nm`·`rcept_no` 만 새로 뒀다. 확인 결과 LG이노텍 후보 750조각 중 133이 감사보고서에서 왔고 최신 연도 후보 198 중 43이 그렇다.
`doc_group` 을 `audit` 으로 새로 둔 이유: `build_fs.py` 가 `doc_group='periodic'` 으로 거르므로 감사보고서에 딸린 재무제표가 그 기업의 주 재무제표로 다시 잡히는 일을 막는다.
되돌리기: `--revert`. 매핑을 `data/audit_docs.csv` 에 남기고, 그 파일이 없어도 `file_path` 가 부모 폴더를 가리키므로 부모를 되찾는다. 관문 6겹이 두 경로 모두 415/415 로 확인했다. 매핑 CSV 는 파생물이라 git 에 안 올린다.
기각한 대안: 그대로 두는 방식(감사의견 질의의 근거 표시가 계속 틀린다). `section` 에 구분 칼럼만 두는 절충(근거 공시 단위가 여전히 사업보고서라 표시 문제가 안 풀린다).

### 2026-08-31 CLOVA 임베딩 속도를 실측했다 — 분당 26건
결정: 저장소의 추정 셋을 폐기하고 실측값으로 대체한다. 실효 분당 26건이다. `docs/PLAN.md` 의 분당 25건이 거의 맞았고 `src/clova.py` 의 분당 88건은 짧은 버스트 값이라 지속되지 않는다.
실측: 500조각 · 16.3분 · 성공 420 · 실패 80 · 429 누적 435 · 최종 간격 0.45s. 표본은 `chunk_id` 앞에서부터 500개이고 전체 평균보다 12% 무겁다. 비용 192원.
실패의 성격: 80건 전부 HTTP 429 다. 인증·형식 오류는 없다. 5회 재시도를 다 쓰고도 실패한 것이라 다음 실행에서 자동으로 다시 잡힌다. `build_embedding.py` 가 `embedding IS NULL` 만 처리하기 때문이다. 실패가 길이에 몰린다. 4,000~5,000자 구간 실패율 26%, 1,000자 미만은 8% 다. 호출 제한이 건수가 아니라 토큰량 기준이라는 `src/chunk.py` 의 관측과 맞는다.
전수 소요: 1차 통과 110시간, 실패 16%의 2차 통과 18시간, 3차 3시간으로 약 130시간이다. 연속 가동이면 5.4일이고 취침 8시간만 쓰면 16일이다. 3~4일 안에 끝난다는 기대는 실측과 맞지 않는다.

### 2026-09-02 서버 상태 기재를 정정한다 — 개설만 됐고 아무것도 안 올라가 있다
결정: `docs/PLAN.md` 의 "사전 — 서버 구축·파이프라인 관통·자동 재시작 완료" 를 둘로 가른다. "사전 — 서버 개설: 완료" 와 "사전 — 파이프라인 관통·자동 재시작: 미착수" 다. W9 의 "지금 서버에는 CLOVA 직결 코드만 있다" 도 지운다.
이유: 2026-09-02 에 원격 데스크톱으로 확인하니 서버에 우리 폴더가 없고 실행 중인 것도 없었다. 서버 컴퓨터를 만들고 접속을 확인한 것까지가 된 것이고 그 위의 작업은 시작 전이다. "완료" 로 적힌 것을 믿고 넘어가면 마감 직전에 발견하게 된다. 2026-08-13 "Windows 유지에 따라 서비스 자동 재시작 검증을 선행 과제로 둔다" 에서 그 검증을 선행 과제로 삼기로 했는데 실제로는 안 했다.
확인한 것: 서버 컴퓨터 있음 (NCP Windows Server 2022 · 공인 IP 211.188.57.111) · 원격 데스크톱 접속 됨 · Python 설치됨 · 우리 폴더 없음 · 실행 중인 프로세스 없음.
남은 위험: 자동 재시작이 아직 검증 안 됐다. 09.07~09.20 14일간 한 번도 안 꺼져야 하는데 Windows 는 systemd 가 없어 NSSM 이나 작업 스케줄러를 써야 하고 참고 사례가 적다. 마감 직전에 발견하면 대응 시간이 없다는 것이 2026-08-13 결정의 근거였고, 그 근거가 여전히 유효하다.

### 2026-09-02 HyperCLOVA X 생성 모델을 src/hcx.py 로 붙인다
결정: `src/hcx.py` 에 `Chat` 클래스를 두고 생성 모델 호출을 여기로 모은다. `clova.py` 의 `Embedder` 와 짝이다. 주소는 `https://clovastudio.stream.ntruss.com/v3/chat-completions/HCX-005` 이고 인증은 `.env` 의 `CLOVA_API_KEY` 를 그대로 쓴다.
이유: 대회 규칙상 생성 모델은 HyperCLOVA X 만 쓸 수 있고, 지금까지 `clova.py` 에는 임베딩(`Embedder`) 밖에 없었다. 답변을 만들 통로가 아예 없는 상태였다. 첫 호출이 1.1초에 성공했다. 별도 키 발급이 필요 없다는 것도 확인됐다.
스트리밍을 안 쓴 이유: 콘솔 예시 코드는 `Accept: text/event-stream` 으로 답을 조금씩 받는다. 사람이 화면에서 글자가 흘러나오는 것을 볼 때 쓰는 방식이다. 평가자는 GET 을 한 번 보내고 완성된 JSON 을 받아 가므로 중간 과정을 보여 줄 곳이 없다. `Accept: application/json` 으로 한 번에 받되, 서버가 그래도 스트리밍으로 주면 이어 붙이는 처리를 넣었다.
기각한 대안: `requests` 를 쓰는 콘솔 예시 코드를 그대로 가져오는 방식. `clova.py` 가 표준 라이브러리만 쓰고 있어 의존성을 늘리지 않았다.

### 2026-09-02 Corpus 조회에 c.corp_code 조건을 넣는다 — 지우지 말 것
결정: `src/retrieval.py` 의 `Corpus.__init__` 조회에 `c.corp_code IN (SELECT corp_code FROM document WHERE corp_name IN (...))` 를 넣는다. 기존의 `d.corp_name IN (...)` 도 그대로 둔다. 겹쳐 보이지만 지우면 안 된다.
이유: `d.corp_name` 은 document 표의 칸이고 chunk 표에는 없다. 그래서 SQLite 는 어느 조각이 그 기업 것인지 미리 알 수 없어 chunk 171,564행을 전부 꺼내 document 와 이어 붙여 본다(`SCAN c`). 조각마다 3,072차원 벡터 12KB 가 붙어 있어 약 2GB 를 읽는다. chunk 에는 `corp_code` 칸과 색인 `ix_chunk_corp` 가 있으므로 조건에 넣으면 색인을 타고(`SEARCH c USING INDEX ix_chunk_corp`) 그 기업 조각만 집는다.
실측 (삼성전자 · annual · min_chars 200): 조건 없이 101.3초, 조건 넣고 1.1초. 95배. 돌아온 조각 집합은 완전히 동일(830행, chunk_id 대조 일치). 임베딩 작업이 같은 DB 에 쓰는 중에 잰 값이다.
파이프라인 영향: API 첫 요청 왕복이 133.5초에서 24.8초로 줄었다. 같은 기업 두 번째 요청은 8초대로 변화 없다. 시간의 대부분이 HCX 생성 호출(5~9초)이고 검색은 0.2초 안쪽이다.
기각한 대안: 기동할 때 70개사를 미리 메모리에 올려 두는 방식(`WARM`). 적재가 1초면 미리 올릴 값어치가 없고, 8GB 서버에서 약 700MB(70 × 830조각 × 12KB)를 상시 붙드는 것이 오히려 위험하다. `WARM` 은 비워 둔다. 캐시(`MAX_CORP`)는 남긴다. 만들어 둔 것을 버릴 이유는 없다.
남은 위험: 이 조건은 없어도 결과가 같으므로 나중에 "중복이라 지운다" 가 나오기 쉽다. `Corpus` 클래스 설명에 지우지 말라고 적어 두었다.

### 2026-09-03 CLOVA 임베딩 호출 한도를 실측하고 대기 방식을 고친다
결정: `src/clova.py` 의 `Embedder` 가 응답 머리글의 잔여 예산을 읽고, 429 를 만나면 서버가 알려준 리셋 시각까지 기다리게 한다. 호출 전에도 남은 토큰과 던질 글의 길이를 견줘 모자라면 미리 기다린다. `MAX_GAP` 을 0.5초에서 1.0초로 올린다.
실측한 한도 (2026-09-03 · 응답 머리글에서 직접 읽음): `x-ratelimit-limit-requests 60` · `x-ratelimit-limit-tokens 40000` · `x-ratelimit-reset-tokens 36s`. 분당 60회, 분당 40,000 토큰이고 60초마다 채워진다. 묶이는 쪽은 토큰이다. 짧은 글은 즉시 통과하고 1,200자는 429 로 다섯 번 연속 거부됐다.
고친 이유: 기존 코드가 `Retry-After` 머리글을 찾는데 CLOVA 는 그것을 주지 않는다. 그래서 늘 대체 경로로 가서 최대 4초만 쉬었다. 리셋은 최대 60초다. 다섯 번 재시도를 7.5초 안에 다 쓰고 조각을 실패로 기록한 뒤 다음 조각으로 넘어가, 예산이 없는 50초 동안 조각 서너 개를 헛되이 태웠다.
효과 (같은 단계 3 · XII 상세표에서 직접 비교): 분당 3.4건 → 9.7건. 2.8배. 10.4분에 100건 처리해 실패 0건. 고치기 전에는 실패가 계속 나왔다.
9.7건이 천장인 이유: 단계 3 조각의 평균이 2,986자다. 분당 9.7건이면 분당 28,964자를 쓰는 것이고, 이것이 40,000 토큰 한도에 해당한다. 글자당 약 1.38 토큰이다. 표에 숫자와 구분자가 많아 토큰이 글자보다 많이 나온다. 예산을 다 쓰고 있으므로 더 빠르게 만들 여지가 없다.
소요 시간 계산을 고친다: 지금까지 "남은 조각 수 ÷ 분당 처리 건수" 로 계산해 왔는데 이 계산은 성립하지 않는다. 조각마다 길이가 다르고 한도는 토큰 기준이기 때문이다. "남은 글자량 ÷ 분당 28,964자" 로 계산해야 한다.
남은 글자량 실측 (2026-09-03 15:36): 단계 3 은 2,992조각 8,935,361자(평균 2,986자), 단계 4 는 94,400조각 243,807,617자(평균 2,583자), 단계 5 는 15,442조각 53,903,364자(평균 3,491자). 합계 112,834조각 306,646,342자.
남은 위험: 이 글자량으로 계산하면 176시간이 걸린다. 마감(09-06)까지 남은 시간의 두 배다. 전수 완료는 불가능하다. 무엇을 포기할지 정해야 한다.

### 2026-09-03 토큰 어림 비율을 글자당 0.75 로 교정한다
결정: `src/clova.py` 의 `TOK_PER_CHAR` 를 1.0(암묵)에서 0.75 로 바꾼다. 호출 전 예산 확인에서 필요량을 `글자 수 × 0.75` 로 잡는다.
실측 근거: 2026-09-03 16:29 에 응답 머리글의 잔여 토큰을 12초 간격으로 읽었다. 16:29:02 에 31,293, 16:29:14 에 11,555 로 12초 동안 호출 10번에 19,738 토큰이 줄었다. 한 번에 1,974 토큰이고 그때 처리 중인 조각의 평균이 2,986자이므로 글자당 0.66 토큰이다.
앞서 낸 값을 정정한다: 같은 날 처리 속도에서 역산해 "글자당 1.38 토큰" 이라고 기록했는데 정확히 반대로 틀렸다. 역산에 쓴 처리 속도(분당 9.7건)가 10분짜리 표본이었고, 저장이 100건 단위라 짧은 창으로 재면 값이 크게 흔들린다. 머리글을 직접 읽는 쪽이 옳다.
효과 (같은 단계 3 · XII 상세표에서 연속 측정): 분당 3.4건 → 9.7~15건 → 28.4건. 30분 누적이고 마지막 두 구간이 28.3과 28.4로 안정적이다. 프로그램 자체 기록에서 900건 처리에 실패 0건, 429 누적 0회, 간격 0.00초다. 미리 기다려 429 자체를 피하고 있다.
아울러 `PYTHONUNBUFFERED` 를 걸고 띄운다. `run_embedding.py` 가 자식으로 부르는 `build_embedding.py` 의 출력이 버퍼에 갇혀 진행 기록을 못 보고 있었다. `-u` 는 부모에만 걸리므로 환경변수라야 자식까지 간다.
