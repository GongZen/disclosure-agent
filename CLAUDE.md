# 공시 Agent — 프로젝트 규칙

제10회 2026 미래에셋증권 AI Festival 참가 프로젝트.
행동 규칙은 전역 `~/.claude/CLAUDE.md`를 따른다. 이 문서는 이 프로젝트에만 적용되는 사항이다.

---

## 절대 금지 — 위반 시 실격 또는 규칙 위반

아래 4개는 문서를 찾아보기 전에 이미 알고 있어야 하는 제약이다. 제안·설계·구현 어느 단계에서든 여기에 걸리면 즉시 중단하고 알린다.

### 1. HyperCLOVA X 외 LLM 사용 금지

생성 모델은 HyperCLOVA X만 사용한다. 다른 LLM을 사용하면 평가 대상에서 제외된다.

- OpenAI, Gemini, Claude, Llama 등 어떤 외부 LLM도 파이프라인에 넣지 않는다.
- 임베딩·리랭커·형태소 분석기는 제약 대상이 아니다. 생성 모델에만 걸리는 제약이다.

### 2. 제공 코퍼스 외 데이터 사용 금지

주최측이 제공한 공시 데이터 외의 어떤 데이터도 사용하지 않는다.

- 뉴스, 애널리스트 리포트, 위키, 웹 크롤링 결과 모두 금지.
- "검색 성능을 높이기 위해 외부 정보를 보강하자"는 방향 자체가 위반이다.

### 3. 외부 공시 API 실시간 호출 금지

OpenDART 등 외부 공시 API를 실시간으로 호출하지 않는다.

### 4. 공시에 근거 없는 미래 예측·투자 의견 생성 금지

- 주가 전망, 투자 추천, 매수·매도 의견을 생성하지 않는다.
- 공시에 없는 내용은 추측하지 않는다.
- 확인할 수 없으면 "확인할 수 없음 / 공시에서 확인되지 않음"을 명시한다. 이것은 감점이 아니라 평가 항목이다.

---

## 라우팅 — 어떤 문서를 볼 것인가

| 확인이 필요한 사항 | 참조 |
|---|---|
| 지금 무엇을 할 차례인가, 작업 순서와 의존 관계 | `docs/PLAN.md` (W1~W10) |
| Agent가 질의를 어떻게 처리하는가 | `docs/PIPELINE.md` (S1~S11) |
| 용어의 뜻, 아직 안 정해진 정의 | `docs/GLOSSARY.md` (D1~D7) |
| 데이터가 실제로 어떻게 생겼는가, 함정과 실측값 | `docs/DATASET.md` |
| 과제 정의, 데이터 범위와 규칙, 평가지표, 제출물, API 스키마, 일정 | `docs/BRIEF.md` |
| CLOVA Studio 모델·임베딩·기능 스펙 | `docs/BRIEF.md` |
| 과거에 내린 결정과 그 이유 | `DECISIONS.md` |
| 팀 아이디어, 브레인스토밍 원본 | `MEMO.md` |
| 대회 과제 원본 | `reference/미래에셋증권 AI Festival 과제 자료_공시 Agent.pdf` |

## 기호

세 가지를 구분해서 쓴다. 섞지 않는다.

| 기호 | 뜻 | 문서 |
|---|---|---|
| `S1~S11` | Agent가 질의를 처리하며 밟는 실행 단계 | `docs/PIPELINE.md` |
| `D1~D7` | 아직 답이 없는 정의 질문 | `docs/GLOSSARY.md` |
| `W1~W10` | 우리가 순서대로 해야 할 작업 | `docs/PLAN.md` |

---

## 현재 상태

- 데이터셋 미확보. 설계는 전부 가정 위에 있다.
- 소스코드 없음. 기획 단계.
